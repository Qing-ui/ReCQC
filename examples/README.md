# Example data

The `data` directory contains example SDF and spreadsheet files for learning and
reproducibility checks. Do not overwrite these files with analysis output.

Large production datasets should be stored in a research-data repository or a
versioned release asset rather than committed repeatedly to Git history.
